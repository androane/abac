mutation UpdateClientInvoiceItem(
  $clientUuid: String!
  $invoiceUuid: String!
  $invoiceItemInput: InvoiceItemInput!
) {
  updateClientInvoiceItem(
    clientUuid: $clientUuid
    invoiceUuid: $invoiceUuid
    invoiceItemInput: $invoiceItemInput
  ) {
    error {
      ...Error
    }
  }
}
