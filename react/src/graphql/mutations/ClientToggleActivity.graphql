mutation ToggleClientActivity($clientUuid: String!, $clientActivityUuid: String!) {
  toggleClientActivity(clientUuid: $clientUuid, clientActivityUuid: $clientActivityUuid) {
    error {
      ...Error
    }
  }
}
