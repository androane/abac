mutation ReportGenerateUserReport(
  $year: Int!
  $month: Int!
  $categoryCodes: [String!]!
  $userUuids: [String!]!
) {
  generateReport(year: $year, month: $month, categoryCodes: $categoryCodes, userUuids: $userUuids) {
    downloadUrl
  }
}
