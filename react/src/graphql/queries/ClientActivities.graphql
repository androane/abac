#import "./fragments/Activity.graphql"

query ClientActivities($clientUuid: String!) {
  client(uuid: $clientUuid) {
    uuid
    activities {
      date
      quantity
      minutesAllocated
      activity {
        ...Activity
      }
    }
  }
}
